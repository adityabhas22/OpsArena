# LLM gateway credentials injected by the evaluator/runtime
HF_TOKEN=
API_BASE_URL=
MODEL_NAME=

# Optional local override for OpsArena server endpoint used by inference.py
BASE_URL=http://localhost:8000
